import React from 'react';
import { Box, Typography } from '../mui';

function SpecFilter() {
  return (
    <Box sx={{
      m: 1, p: 1, border: 1, borderColor: 'grey.400',
    }}
    >
      <Typography>Specifications Filter</Typography>
    </Box>
  );
}

export default SpecFilter;
