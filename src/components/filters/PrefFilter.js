import React from 'react';
import { Box, Typography } from '../mui';

function PrefFilter() {
  return (
    <Box sx={{
      m: 1, p: 1, border: 1, borderColor: 'grey.400',
    }}
    >
      <Typography>Preference Filter</Typography>
    </Box>
  );
}

export default PrefFilter;
